services:
  expo-app:
    build: .
    ports:
      - "6000:6000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0
    command: npx expo start --host lan --port 6000